language: elixir

env:
  global:
    - ORIENTDB_USER=root
    - ORIENTDB_PASS=root
  matrix:
    - ORIENTDB_VERSION="2.2.0-beta"

elixir:
  - 1.2.3

otp_release:
  - 18.2.1

before_install:
  - chmod +x ./ci/ci.sh
  - ./ci/ci.sh
  - export PATH="$(pwd)/tmp:$PATH"

script:
  - mix test.all
